WIFI_SSID=
WIFI_PASSWORD=

# Start auto mouse movement on boot. 1 = on (default if unset), 0 = off.
# Runtime commands automove=START / automove=STOP work regardless.
AUTOMOVE_AUTOSTART=1
MOUSE_MOVE_INTERVAL=60
